<Window x:Class="ScreenShotCapture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Active Window Capture" Height="145" Width="535"
        Loaded="WindowLoaded" Icon="favicon.ico">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <Label>Folder to save screenshots:</Label>
                <TextBox Width="300" Text="{Binding Path=Folder}"></TextBox>
                <Button Content="Change" Click="selectFolder"></Button>
            </StackPanel>
            <StackPanel Margin="5">
                <Label HorizontalAlignment="Left" FontSize="10"> Print Screen  : Capture 활성화 On / Off</Label>
                <Label HorizontalAlignment="Left" FontSize="10"> Mouse Left Up : Window Screen Capture</Label>
                <Label HorizontalAlignment="Left" FontSize="10"> https://github.com/jinoSon/ScreenShotCapture</Label>
            </StackPanel>
            <StackPanel Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
